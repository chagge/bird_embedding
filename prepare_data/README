Pipe line of preparing data

1. subset_data.py :  take a subset of eBird checklists 
2. extract_counts.py :  fill X values with the mean of positive observations, and extract count matrix from the data.  
3. extract_obs_covarates.py : extract covariates from each checklist 
4. separate_sets.py : separate the dataset into different data folds

The data ready for use has two matrices, counts and obs_cov, for training, validation, and testing. 
